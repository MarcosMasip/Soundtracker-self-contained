Place movie JSON fixture files here (one per movie) named <id>.json.
Each file should serialize a Movie entity structure compatible with the JPA model.
An optional index.json can be added but is not required.
